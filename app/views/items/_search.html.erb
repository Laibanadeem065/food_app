<%= form_with(url: items_path, method: :get, local: true, class: "d-flex") do |form| %>
  <%= form.text_field :query, value: params[:query], class: "form-control me-2", type: "search", placeholder: "Search", aria_label: "Search" %>
<% end %>

